using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class F00Goomba : EnemyManager
{
    // Start is called before the first frame update
    void Start()
    {
        enemyId = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
